 <% if user_signed_in? %>
  <% if current_user.has_role? :admin%>
    <%= link_to "NEW POST", new_post_path %>
  <% end %>
<% end %>
<% @posts.each do |post| %>
  <p><%=post.created_at%></p>
  <p><%=post.text%>
  <%= link_to "READ MORE", post_path(post) %>
<%end%>